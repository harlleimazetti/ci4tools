<div class="form-group">
  <label class="form-label" for="{{name}}">{{label}}</label>
  <textarea class="form-control" name="{{name}}" id="{{name}}" rows="5">
    <?php echo ${{table}}->{{name}} ?>
  </textarea>
</div>