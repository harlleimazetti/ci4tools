<div class="form-group">
  <label class="form-label" for="{{name}}">{{label}}</label>
  <div class="bg-white shadow-inset-2">
    <input type="text" name="{{name}}" id="{{name}}" value="<?php echo ${{table}}->{{name}} ?>" class="form-control bg-transparent" placeholder="{{label}}">
    <span class="help-block"></span>
  </div>
</div>